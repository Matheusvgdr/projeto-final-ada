import { NomeAbreviadoPipe } from './nome-abreviado.pipe';

describe('NomeAbreviadoPipe', () => {
  it('create an instance', () => {
    const pipe = new NomeAbreviadoPipe();
    expect(pipe).toBeTruthy();
  });
});
